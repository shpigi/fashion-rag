[project]
name = "fashion-rag"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "numpy",
    "pandas",
    "Pillow",
    "torch",
    "transformers",
    "tqdm",
    "streamlit",
    "google-cloud-bigquery",
    "google-cloud-storage",
    "gcsfs",
    "universal-pathlib",
    "db-dtypes",
]